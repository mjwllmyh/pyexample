def foo():
    """   
    :Parameters:
        dest : 
            The destination path for the copy.
        symlinks : `bool`
            If True, copy the target of each symlink.  If False,
            copy the symlink itself.
         
    :Returns:
        A list of objects copied.
 
    :Rtype:
 
        `list`
 
    :Raises `OSError`:
 
        If the destination directory already exists.
 
    :Raises `IOError`:
 
        If you do not have permission to copy to the destination.
    """
    pass
